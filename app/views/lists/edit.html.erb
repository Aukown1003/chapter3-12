<h1>編集画面</h1>
<!--update_list はroutesの,as: 'update_path'で設定した。patchは更新の意味-->
<!--resources :listsに変更。routesにupdateはlist + patchとあるので変更-->
<%= form_with model: @list, url: list_path(@list.id), method: :patch do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>本文</h4>
  <%= f.text_area :body %>
  <%= f.submit '保存' %>
<% end %>
